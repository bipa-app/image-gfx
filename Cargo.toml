[package]
name = "image-gfx"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.23", default-features = false }

[dev-dependencies]
image = { version = "0.23", default-features = false, features = ["png"] }